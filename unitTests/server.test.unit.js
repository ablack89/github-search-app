var assert = require('assert');

describe('1', function() {
  describe('#equals()', function() {
    it('should return true when given value 1', function() {
      assert.equal(1, 1);
    });
  });
});
